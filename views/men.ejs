<%- include('layouts/header') %>
<%- include('layouts/navbar') %>

<main class="category-page-wrapper">
    <!-- Men's Collection Hero Banner -->
    <section class="category-hero men-hero">
        <div class="category-hero-bg">
            <img src="https://images.unsplash.com/photo-1507679799987-c73779587ccf?q=80&w=2071&auto=format&fit=crop" alt="ALVORA Men Collection">
            <div class="hero-overlay"></div>
        </div>
        <div class="category-hero-content center">
            <span class="editorial-subheading">SARTORIAL PRECISION</span>
            <h1 class="category-title">The Men's Collection</h1>
            <p class="category-tagline">Architectural tailoring, refined cashmere, and understated luxury designed for the modern gentleman.</p>
        </div>
    </section>

    <!-- Sub-Category Navigation Bar -->
    <div class="category-nav-bar">
        <div class="container">
            <ul class="subnav-list">
                <li><a href="/products/category/men" class="subnav-link active">ALL MEN</a></li>
                <li><a href="/products?category=men&subcategory=suits" class="subnav-link">Suits & Blazers</a></li>
                <li><a href="/products?category=men&subcategory=knitwear" class="subnav-link">Cashmere Knitwear</a></li>
                <li><a href="/products?category=men&subcategory=outerwear" class="subnav-link">Coats & Jackets</a></li>
                <li><a href="/products?category=men&subcategory=shirts" class="subnav-link">Tailored Shirts</a></li>
                <li><a href="/products?category=men&subcategory=trousers" class="subnav-link">Trousers</a></li>
            </ul>
        </div>
    </div>

    <!-- Men's Product Display Section -->
    <section class="section-padding category-products-section">
        <div class="container">
            <!-- Filter & Sort Bar -->
            <div class="category-toolbar">
                <div class="toolbar-info">
                    <span class="product-count">
                        Showing <strong><%= typeof products !== 'undefined' ? products.length : 0 %></strong> Men's Creations
                    </span>
                </div>
                <div class="toolbar-actions">
                    <form action="/products/category/men" method="GET" class="sort-form">
                        <select name="sort" class="sort-select" onchange="this.form.submit()">
                            <option value="newest" <%= typeof currentSort !== 'undefined' && currentSort === 'newest' ? 'selected' : '' %>>Newest Arrivals</option>
                            <option value="price-asc" <%= typeof currentSort !== 'undefined' && currentSort === 'price-asc' ? 'selected' : '' %>>Price: Low to High</option>
                            <option value="price-desc" <%= typeof currentSort !== 'undefined' && currentSort === 'price-desc' ? 'selected' : '' %>>Price: High to Low</option>
                        </select>
                    </form>
                </div>
            </div>

            <!-- Product Grid -->
            <div class="product-grid">
                <% if (typeof products !== 'undefined' && products && products.length > 0) { %>
                    <% products.forEach(product => { %>
                        <div class="product-card" data-id="<%= product._id %>">
                            <div class="card-image-wrapper">
                                <a href="/products/<%= product.slug || product._id %>">
                                    <img src="<%= product.images && product.images.length > 0 ? product.images[0] : '/images/placeholder.jpg' %>" alt="<%= product.name %>" class="primary-image">
                                    <% if (product.images && product.images.length > 1) { %>
                                        <img src="<%= product.images[1] %>" alt="<%= product.name %>" class="secondary-image">
                                    <% } %>
                                </a>

                                <!-- Wishlist Heart Toggle -->
                                <button type="button" class="wishlist-heart-btn" data-product-id="<%= product._id %>" aria-label="Add to Wishlist">
                                    <i class="ri-heart-line"></i>
                                </button>

                                <div class="quick-add-overlay">
                                    <a href="/products/<%= product.slug || product._id %>" class="btn-quick-view">VIEW DETAILS</a>
                                </div>
                            </div>

                            <div class="card-details">
                                <span class="card-brand"><%= product.brand || 'ALVORA' %></span>
                                <h3 class="card-name"><a href="/products/<%= product.slug || product._id %>"><%= product.name %></a></h3>
                                <div class="card-price">
                                    $<%= product.price ? product.price.toLocaleString('en-US', { minimumFractionDigits: 2 }) : '0.00' %>
                                </div>

                                <% if (product.sizes && product.sizes.length > 0) { %>
                                    <div class="card-sizes">
                                        <% product.sizes.forEach(size => { %>
                                            <span class="size-pill"><%= size %></span>
                                        <% }); %>
                                    </div>
                                <% } %>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="no-products-found center">
                        <i class="ri-shirt-line empty-icon"></i>
                        <h2>Men's Collection Coming Soon</h2>
                        <p>Our tailoring atelier is finalizing the latest menswear arrivals.</p>
                        <a href="/products" class="btn btn-primary">EXPLORE ALL COLLECTIONS</a>
                    </div>
                <% } %>
            </div>
        </div>
    </section>

    <!-- Men's Editorial Feature Banner -->
    <section class="category-editorial-banner">
        <div class="container">
            <div class="editorial-box">
                <div class="editorial-image">
                    <img src="https://images.unsplash.com/photo-1617137984095-74e4e5e3613f?q=80&w=1287&auto=format&fit=crop" alt="Men's Bespoke Tailoring">
                </div>
                <div class="editorial-info">
                    <span class="editorial-tag">ATELIER TAILORING</span>
                    <h2>The Bespoke Cut</h2>
                    <p>Designed with natural shoulders, floating canvas construction, and pure Italian wool. Built to provide structural elegance without sacrificing fluid movement.</p>
                    <a href="/products?category=men&subcategory=suits" class="btn btn-outline">EXPLORE TAILORING</a>
                </div>
            </div>
        </div>
    </section>
</main>

<%- include('layouts/footer') %>
<%- include('layouts/scripts') %>

